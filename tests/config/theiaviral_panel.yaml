# TheiaValidate v2 preset: theiaviral_panel
# Auto-generated from validation-criteria-v4-2-0-theiaviral_panel.tsv
#
# KEYLESS preset: the Terra entity-id column (entity:<table>_id) differs per
# export, so pass the key at run time, e.g.:
#   theiavalidate validate T1 T2 --preset theiaviral_panel \
#     --key1 entity:<table1>_id --key2 entity:<table2>_id
# (or --key <col> when both tables share one key column).

columns:
  assembled_viruses:
    method: exact
  identified_organisms:
    method: exact
  fastq_scan_num_reads_clean1:
    method: exact
  fastq_scan_num_reads_clean2:
    method: exact
  fastq_scan_num_reads_raw1:
    method: exact
  fastq_scan_num_reads_raw2:
    method: exact
  kraken_percent_human_clean:
    method: exact
  kraken_percent_human_raw:
    method: exact
